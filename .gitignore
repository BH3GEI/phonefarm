# 密钥
secrets.env

# 编译产物
/phonefarm
/ocr
src/target/

# 运行截图与原始XML dump(本地重资产账本,不入库;log.jsonl/ctx.log 保留)
tasks/**/*.jpg
tasks/**/*.png
tasks/**/*.xml
tasks/**/*.xml.gz
tasks/genshin_*
tasks/phonefarm_smoke
tasks/test_*

.DS_Store

# TFLite benchmark_model 真机二进制 (tools/tflite/fetch.sh 拉取)
tools/tflite/android_aarch64_benchmark_model*

# refbench 实跑原始产物 (大 trace, 证据已提交到 HGamey/refbench)
loop_v1/runs_refbench/

# 系统参数闭环的实跑产物 (大 trace, 只留 JSON 证据入库)
loop_v1/runs_sysparam/**/trace.txt
loop_v1/runs_sysparam/**/*.txt.gz
loop_v1/auto/__pycache__/
loop_v1/runs_graylayer/
loop_v1/runs_sysparam/**/env.txt
loop_v1/runs_sysparam/**/*.png
# 裸帧 (负载自检用, 每张 13MB); 判定结论在 spin_check.json 里, 够作证
loop_v1/runs_sysparam/**/*.raw
