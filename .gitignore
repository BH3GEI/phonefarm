# 密钥
secrets.env

# 编译产物
/phonefarm
/ocr
src/target/

# 运行截图与原始XML dump(本地重资产账本,不入库;log.jsonl/ctx.log 保留)
tasks/**/*.jpg
tasks/**/*.png
tasks/**/*.xml
tasks/**/*.xml.gz
tasks/genshin_*
tasks/phonefarm_smoke
tasks/test_*

.DS_Store
