---
description: Phonefarm 核心研发规范、非侵入性约束、自动化目标与会话生命周期金科玉律
globs: **/*
alwaysApply: true
---

# Phonefarm 核心研发规范与金科玉律

时刻严格遵守以下纪律与目标：

## 核心总目标
实现 phonefarm 虚拟手柄 (uinput / gamepad) 注入基础设施，在红魔 11 Pro 原神实机环境下进行端到端测试并修复 bug，再次端到端验证后提交 PR 并 merge。
然后方案自选，达成让 agent 帮我过剧情、做任务（不涉及战斗等）的目标。

### farmagent 的定位必须是全自主闭环：
- 剧情推进：通过 PC 端抓取的视觉画面识别剧情字幕区域与分支卡片，自主注入确认按键快进文本，选择分支继续剧情；
- 任务交互：巡视视野内交互标记（如 NPC、调查点、机关提示），自主对齐视角并触发交互；
- 大世界任务与跑图：遇到远距离任务或地形阻碍时，完全由 Agent 自主通过地图与任务指引闭环脱困寻路，绝不依赖人工干预。

### 1. 宿主机全自动生命周期编排（PC 侧控制）
- 按需拉起：Agent 启动时，由 PC 端发出标准唤醒指令（input keyevent WAKEUP 并滑动解锁），唤醒并确保原神在前台。
- 免侵入流式注入：PC 端在内存中启动 adb shell hid - 标准管道，建立虚拟 Xbox 手柄连接；所有按键、摇杆推杆均通过标准输入流式下发。
- 按需收尾与自动锁屏：任务完成或退出时，PC 端主动断开手柄标准输入（系统自动注销虚拟设备，手机端零进程残留），并强制杀死游戏进程（am force-stop com.miHoYo.Yuanshen），随后下发休眠键将手机恢复锁屏状态（input keyevent 26）。

### 2. Agent 全自主剧情与非战斗任务闭环
Agent 在 PC 端通过纯 ADB 截图执行高频轻量图像分析，完全不依赖人工干预，覆盖以下三大自主行为：
- 剧情自动快速推进与选项抉择：
  - 侦测底部字幕对话框与右下角技能栏状态（判定处于剧情会话）；
  - 以 ~300ms 节奏连续注入手柄 A 键推进对白；
  - 侦测右侧分支选项卡片（◆），自动选中并确认推进剧情分支；
  - 遇过场动画（CG / 黑屏加载）自动静默等待，直至场景重现。
- NPC 与目标交互自主触发：
  - 侦测视野内的交互标记（如 [X] 提示、NPC 名称、调查/机关点）；
  - 自主注入 X 键发起对话或触发机关，无缝衔接进入剧情模式。
- 任务导航与自主脱困机制：
  - 当任务距离较远或处于水面/障碍卡死状态时，Agent 自主通过地图/任务追踪界面定位最近的传送锚点并触发传送；
  - 传送落地后，根据屏幕任务指引图标（菱形目标标）利用右摇杆对齐视野方向，左摇杆向前奔跑推进，自主接近目标触发点。

### 3. 模型与资源纪律
- 极度节约，拒绝浪费（使用最廉价的模型）。
- 本地端侧 2B/4B VLM 优先（Ollama / MLX，严格控制系统占用，显存/内存 ≤2.5GB，绝不拖垮电脑）。
- 云端廉价模型兜底（glm-5.3-flash 禁用深度思考 thinking_disable=true；Kimi 备用）。

### 4. 研发工作流与沟通铁律
- 代码是给人看的，只是机器恰好可以运行。
- 及时 Commit，定期 PR：小步快跑，阶段成果立即规范提交，定期提交与合并 PR，严禁变更大量无序积压。
- 严禁以短测充当长测。真机端到端 30 分钟长测跑满 -> 修复 Bug -> 再次端到端通过 -> PR 并 merge。
- 严禁使用任何 Emoji。
- 英文思考（Think in English），中文严谨汇报。
