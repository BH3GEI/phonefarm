# 感知自愈参数 — 由复盘调用提议,程序按白名单落盘
# 来历: 第1局canvas画面元素列表恒为空,列表差集通道整体失效;
#       自愈切至像素通道并加长定格后,空击误判消失。
# 回退: 使用本参数的一局成绩劣于其上一局时,程序删除本文件。

settle_ms = 2000          # 白名单范围 800~4000
diff_channel = "pixel"    # 白名单取值 "els" | "pixel"
