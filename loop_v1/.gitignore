# 原始 ftrace 每轮约 21MB, 21 轮就是 439MB —— 是证据, 但不进 git。
# 需要离线回放时从归档介质取回 runs/ 再跑 tools/replay_test.sh。
runs/*/trace.txt
runs/*.txt
!runs/snap_*.txt

# 解析产物体积很小, 保留在版本里, 它们才是可读的结论
!runs/*/summary.json
!runs/*/attribution.json
!runs/*/snap_*.txt
!runs/report.json
!runs/report.cmd
